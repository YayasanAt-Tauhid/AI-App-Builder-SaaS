# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
.next/
out/
build/
*.tsbuildinfo

# Wrangler (Cloudflare) local state
.wrangler/

# Local runtime data (D1 shards, R2 blobs, KV) — created at runtime, never committed
apps/api/.data/

# Env files (secrets) — never commit
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# Test / coverage
coverage/
.vitest/
.wrangler-ids

# Vercel build artifacts
.vercel

# OpenNext build output
.open-next
.wrangler
